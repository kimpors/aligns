import Header from '../components/Header';

const links = [ "/", "/plans", "/about"];

const Login = () =>
{
  return (
    <Header links={ links }>
      Login
    </Header>
  )
}

export default Login;
