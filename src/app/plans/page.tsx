import Header from '../components/Header';

const links = [ "/", "/login", "/about"];

const Plans = () =>
{
  return (
    <Header links={ links }>
      Plans
    </Header>
  )
}

export default Plans;
